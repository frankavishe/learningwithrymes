<?xml version="1.0" encoding="utf-8"?>
<!--
  Dev-only: the NestJS backend (specs/03-auth-api.md) runs over plain HTTP
  on the host during local development, and Android 9+ blocks cleartext
  traffic by default. `10.0.2.2` is the Android emulator's alias for the
  host machine's localhost (matches ApiClient's default base URL);
  `localhost`/`127.0.0.1` cover a physical device reached via
  `adb reverse`. Tighten this (drop cleartextTrafficPermitted, or scope to
  a real HTTPS host) before any non-dev build.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
